http:

  routers:

    wordpress:

      rule: "Host(`wordpress.test`)"

      entryPoints:
        - web

      service: wordpress

  services:

    wordpress:

      loadBalancer:

        servers:
          - url: "http://nginx:80"